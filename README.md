# Website Builder

A new project for building websites. Requirements and tech stack are to be defined.

## Project status

**Phase 0 — setup complete:** GitHub repo and project scaffolding in place.

## Quick start

```powershell
cd "C:\Users\user\Desktop\website-builder"
# Commands will be added as the project takes shape
```

## Git workflow

```powershell
git add .
git commit -m "Describe your change"
git push
```

To revert to a previous version, browse commit history on GitHub or run `git log` locally.
